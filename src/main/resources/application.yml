server:
  port: 8080
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/springbootdb?useSSL=false&serverTimezone=UTC
  redis:
    port: 6379
    host: localhost
logging:
  level:
    com.houzer: debug
